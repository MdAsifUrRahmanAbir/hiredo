import 'package:flutter/material.dart';

class BrowserNotification extends StatelessWidget {
  const BrowserNotification({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
